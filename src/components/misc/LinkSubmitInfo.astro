---
interface Props {
	dataType?: "friends";
	class?: string;
}

const { dataType = "friends", class: className } = Astro.props;

const linkText = "https://github.com/mengkuikun/Blog/tree/main/src/data/friends";
---

<div class:list={["text-sm text-white/60", className]}>
    <p>
        如果您已添加本站为友链，并且想加入友链名单，<a target="_blank" href={linkText} class="transition link text-[var(--primary)] font-medium underline">请点击这里提交 PR</a>。
    </p>
    <p class="mt-2">
        我们会定期运行工作流对所有友链站点做连通性测试，欢迎提交 PR 申请交换友链~
    </p>
</div>
